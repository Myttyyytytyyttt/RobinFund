{
  "name": "@nuvem/subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "Nuvem agent and vault subgraph for Robinhood Chain",
  "scripts": {
    "configure": "node scripts/configure-network.mjs",
    "codegen": "graph codegen",
    "build": "graph build",
    "build:robinhood": "pnpm configure && graph codegen && graph build --network robinhood",
    "build:testnet": "graph codegen && graph build --network robinhood-testnet",
    "graph-node:up": "docker compose -f docker-compose.graph-node.yml up -d",
    "graph-node:down": "docker compose -f docker-compose.graph-node.yml down",
    "create:local": "graph create --node http://127.0.0.1:8020 nuvem/robinhood-testnet",
    "deploy:local": "graph deploy --node http://127.0.0.1:8020 --ipfs http://127.0.0.1:5001 --network robinhood-testnet --version-label local nuvem/robinhood-testnet",
    "smoke:local": "node scripts/smoke-query.mjs",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.38.2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "matchstick-as": "^0.6.0",
    "source-map-support": "^0.5.21"
  }
}
